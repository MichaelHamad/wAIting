[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "waiting"
version = "0.1.0"
description = "Audio notification system for Claude Code"
requires-python = ">=3.9"
dependencies = []
